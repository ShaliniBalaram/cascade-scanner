# Copy this to secrets.toml and fill in your values
# On Streamlit Cloud: Settings → Secrets

[neo4j]
uri = "neo4j+s://xxxxx.databases.neo4j.io"  # Neo4j Aura URI
user = "neo4j"
password = "your-password"

[gee]
project_id = "cascade-scanner-gee"
# For service account, paste the JSON content
service_account_json = '''
{
  "type": "service_account",
  "project_id": "cascade-scanner-gee",
  ...
}
'''
